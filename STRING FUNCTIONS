+-------------------------------+
|1. Tidy Up Data:               |
+-------------------------------+
UPPER('hello') → 'HELLO'
LOWER('HELLO') → 'hello'
---------------------------------------
---------------------------------------

+-------------------------------+
| 2. Extract substring:         |
+-------------------------------+
LEFT('abcdef', 3) → 'abc'
RIGHT('abcdef', 3) → 'def'
---------------------------------------
---------------------------------------

+-------------------------------+
|3. Calculating String Length:  |
+-------------------------------+
LENGTH('hello') → 5	
---------------------------------------
---------------------------------------

+-------------------------------+
| 4. Find substring position    |
+-------------------------------+
POSITION('b' IN 'abc') → 2;
LOCATE('b', 'abc') → 2;
---------------------------------------
---------------------------------------

+------------------------------------------------------------------+
| 5. Removing Spaces/specific characters (TRIM(), LTRIM(), RTRIM() |
+------------------------------------------------------------------+
TRIM(' hello ') → 'hello';
LTRIM(' hello') → 'hello';
RTRIM('hello ') → 'hello';
TRIM('x' FROM 'xxhelloxx') → 'hello';

+-------------------------------+
| 6. Concat STRING              |
+-------------------------------+
CONCAT Columns: CONCAT('Hello', ' ', 'World') → 'Hello World';
CONCAT WITH SEPERATOR: CONCAT_WS(',', 'A', 'B', 'C') → 'A,B,C';
---------------------------------------
---------------------------------------


+-------------------------------+
| 7. SUBSTRING                  |
+-------------------------------+
SUBSTRING(str, start, length): SUBSTRING('abcdef', 2, 3) → 'bcd';

+-------------------------------+
| 8. SUBSTRING_INDEX            |
+-------------------------------+
SUBSTRING_INDEX(SUBSTRING_INDEX('a,b,c', ',', 2), ',', -1) → 'b'
--> SUBSTRING_INDEX(str,delimeter,keep first N part)
---------------------------------------
---------------------------------------









